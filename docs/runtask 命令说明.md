# 紫微智控 · runtask 命令说明

> 一个命令， anywhere, anytime!

---

## ✅ 安装完成

**快捷命令**: `runtask`

**无需输入**: `bash /home/admin/Ziwei/scripts/run-task.sh`

---

## 🎯 使用方法

### 方式 1: 交互式输入（推荐）

```bash
# 在任何目录，任何用户
runtask
```

**屏幕提示**:
```
╔════════════════════════════════════════════════════════╗
║          紫微智控 - 一键启动任务                       ║
╚════════════════════════════════════════════════════════╝

[1/3] 请输入任务 ID:
  格式：TASK-日期 - 序号（例如：TASK-20250227-001）
  任务 ID: 

[2/3] 请输入任务名称:
  例如：计算器项目、电商网站、数据分析脚本
  任务名称：

[3/3] 请输入任务描述:
  详细描述任务需求（可选，直接回车跳过）
  任务描述：
```

---

### 方式 2: 命令行参数（快速）

```bash
runtask TASK-20250227-001 "Python 计算器" "创建一个命令行计算器"
```

---

## 📋 功能特性

| 特性 | 说明 |
|------|------|
| **全局命令** | ✅ 任何目录都能用 |
| **所有用户** | ✅ root 或其他用户都能执行 |
| **自动定位** | ✅ 自动切换到 Ziwei 目录 |
| **交互式** | ✅ 逐步提示输入 |
| **容错机制** | ✅ 失败不阻塞流程 |

---

## 🎯 使用场景

### 场景 1: 在家里目录

```bash
~$ runtask
```

### 场景 2: 在临时目录

```bash
/tmp$ runtask
```

### 场景 3: 在其他项目目录

```bash
/projects/other$ runtask
```

### 场景 4: 快速创建

```bash
$ runtask TASK-20250228-002 "电商网站" "React 电商网站前端"
```

---

## 🔧 技术细节

### 命令位置

```
/usr/local/bin/runtask
```

### 实际脚本

```
/home/admin/Ziwei/scripts/run-task.sh
```

### 符号链接

```
/usr/local/bin/runtask -> /home/admin/Ziwei/scripts/run-task.sh
```

---

## 👥 用户权限

### root 用户

```bash
# 直接使用
runtask
```

### 其他用户

```bash
# 直接使用（无需 sudo）
runtask
```

### 权限配置

```bash
# Ziwei 目录权限
drwxr-xr-x scripts/   # 所有用户可读可执行
drwxrwxrwx data/      # 所有用户可写
```

---

## 📝 完整示例

### 交互式创建任务

```bash
$ runtask

╔════════════════════════════════════════════════════════╗
║          紫微智控 - 一键启动任务                       ║
╚════════════════════════════════════════════════════════╝

[1/3] 请输入任务 ID:
  格式：TASK-日期 - 序号（例如：TASK-20250227-001）
  任务 ID: TASK-20250227-001

[2/3] 请输入任务名称:
  例如：计算器项目、电商网站、数据分析脚本
  任务名称：Python 计算器

[3/3] 请输入任务描述:
  详细描述任务需求（可选，直接回车跳过）
  任务描述：创建一个命令行计算器，支持加减乘除

✅ 自动执行所有步骤...
  [1/6] 创建项目（GitHub + Gitee）
  [2/6] 等待仓库准备
  [3/6] 克隆项目到本地
  [4/6] 创建任务文件
  [5/6] 提交并推送到双平台
  [6/6] 启动紫微智控处理流程

✅ 任务启动完成！
```

---

## 🔍 故障排查

### 问题 1: 命令不存在

**检查**:
```bash
which runtask
```

**解决**:
```bash
# 重新安装
sudo bash /home/admin/Ziwei/scripts/install-runtask.sh
```

### 问题 2: 权限错误

**检查**:
```bash
ls -la /usr/local/bin/runtask
ls -la /home/admin/Ziwei/scripts/run-task.sh
```

**解决**:
```bash
# 重新配置权限
sudo chmod +x /usr/local/bin/runtask
sudo chmod -R 755 /home/admin/Ziwei/scripts/
sudo chmod -R 777 /home/admin/Ziwei/data/
```

### 问题 3: 无法写入数据目录

**解决**:
```bash
sudo chmod -R 777 /home/admin/Ziwei/data/
```

---

## 🗑️ 卸载命令

```bash
# 卸载 runtask 命令
sudo bash /home/admin/Ziwei/scripts/uninstall-runtask.sh
```

---

## 🔄 重新安装

```bash
# 重新安装 runtask 命令
sudo bash /home/admin/Ziwei/scripts/install-runtask.sh
```

---

## 📋 快速参考

| 操作 | 命令 |
|------|------|
| **启动任务（交互）** | `runtask` |
| **启动任务（快速）** | `runtask TASK-XXX "名称" "描述"` |
| **查看命令位置** | `which runtask` |
| **查看帮助** | `runtask`（无参数） |
| **卸载** | `sudo bash /home/admin/Ziwei/scripts/uninstall-runtask.sh` |
| **重装** | `sudo bash /home/admin/Ziwei/scripts/install-runtask.sh` |

---

## 🔗 相关文档

- [[一键启动任务指南]] - 完整使用指南
- [[交互式启动说明]] - 交互式输入说明
- [[容错机制更新说明]] - 容错功能
- [[工作流程图]] - 完整工作流程

---

**一个命令 `runtask`， anywhere, anytime!** 🚀
